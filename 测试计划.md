# 企业智能搜索系统 — 测试计划

> **版本**: 1.0 | **日期**: 2026-05-28 | **项目**: zhihu (企业智能搜索系统)

---

## 1. 系统概述

本系统是一个基于 Doubao（DeepSeek）大模型的智能搜索代理，具备多轮推理与决策能力。系统在本地维护了以下可搜索途径：

| 编号 | 搜索途径 | 底层技术 | 数据内容 |
|------|----------|----------|----------|
| S1 | 关系型数据库 | SQLite + SQLAlchemy | 员工、部门、项目、客户、产品、订单、文档 |
| S2 | 向量语义搜索 | ChromaDB + text2vec | 文档内容的语义相似度匹配 |
| S3 | 全文关键词搜索 | Whoosh + jieba 分词 | 文档、日志的精确关键词匹配 |
| S4 | 日志系统 | SQLite + Whoosh 混搜 | 系统运行日志，支持按级别/时间过滤 |
| S5 | 代码仓库 | git grep | 本地 Git 仓库的代码符号搜索 |
| S6 | HR 系统 SDK | SQLAlchemy | 员工搜索、组织架构、下属查询、部门人员 |
| S7 | CRM 系统 SDK | SQLAlchemy | 客户搜索、订单查询、产品搜索 |
| S8 | 文档管理系统 SDK | SQLAlchemy | 文档搜索、按类型获取文档 |
| S9 | 项目管理系统 SDK | SQLAlchemy | 项目搜索、任务查询、团队工作负载 |

**核心流程**：用户提问 → Doubao 模型推理决策 → 选择工具搜索 → 获取结果 → Doubao 再次推理 → 继续搜索或输出最终答案 → 循环直到信息足够。

---

## 2. 测试范围

```
┌──────────────────────────────────────────────────────┐
│                    UI 层 (E2E)                        │
│  Web UI (index.html + app.js)                        │
├──────────────────────────────────────────────────────┤
│                  API 层 (集成测试)                     │
│  /api/chat (SSE), /api/search/direct, /api/stats     │
├──────────────────────────────────────────────────────┤
│                Agent 决策层 (核心逻辑)                 │
│  AgentLoop, ToolRegistry, Tracer, ToolDefinitions    │
├──────────┬──────────┬──────────┬─────────────────────┤
│ 搜索引擎  │ 企业SDK   │ 代码仓库  │ 数据层               │
│ relational│ hr_system│git_manager│ SQLite models     │
│ vector    │ crm_system│         │ ChromaDB           │
│ keyword   │ doc_mgmt │          │ Whoosh index       │
│ log       │ pm_system│           │ seed data          │
└──────────┴──────────┴──────────┴─────────────────────┘
```

---

## 3. 测试环境

| 项目 | 要求 |
|------|------|
| Python | 3.10+ |
| 依赖安装 | `pip install -r requirements.txt` |
| 数据初始化 | `python scripts/build_mock_repos.py && python scripts/seed_all.py` |
| 服务启动 | `python -m uvicorn src.main:app --host 127.0.0.1 --port 8000` |
| API Key | .env 中配置有效的 DEEPSEEK_API_KEY |
| 测试数据量 | 50 员工, 5 部门, 8 项目, 40 任务, 30 客户, 20 产品, 60 订单, 25 文档, 500 日志, 2 代码仓库 |

---

## 4. 单元测试用例

### 4.1 关系型数据库搜索 (search_relational_db)

#### TC-RDB-001: 按员工姓名搜索
- **前提**: 数据库已初始化种子数据（50名员工）
- **输入**: `query="张伟"` (种子数据中的实际姓名)
- **预期**: 返回包含该姓名的员工记录，source="relational_db"，metadata.table="employees"
- **验证点**: 结果数 >=1，title 格式为 "员工: 张伟"，包含 title/email/department_id 字段

#### TC-RDB-002: 按部门名称搜索
- **前提**: 数据库包含 "技术研发部"
- **输入**: `query="技术研发部"`
- **预期**: 返回部门记录，title 格式为 "部门: 技术研发部"
- **验证点**: metadata.table="departments"

#### TC-RDB-003: 按项目名称搜索
- **前提**: 数据库包含 "智慧城市管理平台"
- **输入**: `query="智慧城市"`
- **预期**: 返回匹配项目记录
- **验证点**: metadata.table="projects"，包含 status/priority/budget

#### TC-RDB-004: 无匹配结果
- **前提**: 数据库已初始化
- **输入**: `query="火星殖民计划XYZABC"`
- **预期**: 返回空列表 `[]`

#### TC-RDB-005: top_k 限制
- **前提**: 数据库已初始化
- **输入**: `query="管理"`, `top_k=3`
- **预期**: 返回结果不超过 3 条

#### TC-RDB-006: 按客户行业搜索
- **前提**: 数据库包含 "金融" 行业客户
- **输入**: `query="金融"`
- **预期**: 返回客户记录，metadata.table="customers"

#### TC-RDB-007: 按产品名称搜索
- **前提**: 数据库包含种子产品
- **输入**: `query="数据平台"`
- **预期**: 返回产品记录，metadata.table="products"，包含 category/unit_price/stock_quantity

---

### 4.2 向量语义搜索 (search_vector_store)

#### TC-VEC-001: 语义相似概念搜索
- **前提**: ChromaDB 已索引 25 篇文档
- **输入**: `query="公司安全方面的规定有哪些"`
- **预期**: 返回与信息安全相关的文档片段（如"信息安全管理制度"、"数据安全管理规定"）
- **验证点**: 结果按 score 降序排列，source 包含 "vector_store:documents"

#### TC-VEC-002: 指定 collection 搜索
- **前提**: documents collection 已存在
- **输入**: `query="安全"`, `collection_name="documents"`
- **预期**: 仅从 documents collection 返回结果

#### TC-VEC-003: top_k 限制
- **前提**: ChromaDB 已索引
- **输入**: `query="管理制度"`, `top_k=5`
- **预期**: 返回结果不超过 5 条

#### TC-VEC-004: 空 collection 搜索
- **前提**: 尝试搜索不存在的 collection
- **输入**: `query="test"`, `collection_name="nonexistent_collection"`
- **预期**: 自动创建空 collection，返回空结果 `[]`

#### TC-VEC-005: 跨 collection 搜索（不指定 collection_name）
- **前提**: 存在多个 collection
- **输入**: `query="项目管理"`
- **预期**: 搜索所有 collection，合并并按 score 降序返回

---

### 4.3 全文关键词搜索 (search_keywords)

#### TC-KW-001: 中文分词搜索
- **前提**: Whoosh 已索引文档
- **输入**: `query="数据安全"`
- **预期**: 返回包含 "数据" 和 "安全" 相关的结果
- **验证点**: jieba 分词将输入拆分为 ["数据", "安全"]

#### TC-KW-002: 英文关键词搜索
- **前提**: Whoosh 已索引包含代码关键词的文档
- **输入**: `query="ETL"`
- **预期**: 返回包含 "ETL" 关键词的文档

#### TC-KW-003: 无匹配结果
- **前提**: Whoosh 已索引
- **输入**: `query="星际迷航企业号"`
- **预期**: 返回空列表

#### TC-KW-004: top_k 限制
- **前提**: Whoosh 已索引
- **输入**: `query="管理"`, `top_k=5`
- **预期**: 返回不超过 5 条

#### TC-KW-005: title/content/tags 多字段搜索
- **前提**: Whoosh 索引包含 title、content、tags
- **输入**: `query="制度"`
- **预期**: 在 title/content/tags 三个字段中搜索匹配

---

### 4.4 日志搜索 (search_logs)

#### TC-LOG-001: 按关键词搜索日志
- **前提**: 数据库包含 500 条种子日志
- **输入**: `query="数据库连接超时"`
- **预期**: 返回包含该关键词的日志条目
- **验证点**: source="log_system"，包含 level/module/function_name/timestamp

#### TC-LOG-002: 按日志级别过滤
- **前提**: 数据库包含不同级别的日志
- **输入**: `query=""`, `level="ERROR"`
- **预期**: 仅返回 ERROR 级别的日志
- **验证点**: 所有结果的 metadata.level="ERROR"

#### TC-LOG-003: 按时间范围过滤
- **前提**: 日志时间范围覆盖 2024 年
- **输入**: `query="失败"`, `days_back=7`
- **预期**: 仅返回最近 7 天内的日志（取决于种子数据的基准时间）

#### TC-LOG-004: 组合过滤（关键词 + 级别 + 时间）
- **前提**: 数据库包含种子日志
- **输入**: `query="超时"`, `level="ERROR"`, `days_back=30`
- **预期**: 返回同时满足三个条件的日志

#### TC-LOG-005: 无匹配结果
- **前提**: 数据库包含日志
- **输入**: `query="xyz123_nonexistent_error_code"`
- **预期**: 回退到 Whoosh 关键词搜索补充结果

#### TC-LOG-006: 结果按时间降序排列
- **前提**: 数据库包含多条匹配日志
- **输入**: `query="失败"`, `top_k=10`
- **预期**: 最新日志排在前面

---

### 4.5 代码仓库搜索 (search_code)

#### TC-CODE-001: git grep 搜索函数名
- **前提**: mock_repos 包含 enterprise-api 和 data-pipeline
- **输入**: `query="hash_password"`
- **预期**: 返回 auth.py 中包含 "hash_password" 的行
- **验证点**: source="code_repository"，包含 repo/file/line/content

#### TC-CODE-002: 搜索类名
- **前提**: 代码仓库已初始化
- **输入**: `query="ETLPipeline"`
- **预期**: 返回 etl_main.py 中匹配的行

#### TC-CODE-003: 指定仓库搜索
- **前提**: 存在多个代码仓库
- **输入**: `query="config"`, `repo_name="enterprise-api"`
- **预期**: 仅返回 enterprise-api 仓库中的结果

#### TC-CODE-004: 无匹配结果
- **前提**: 代码仓库已初始化
- **输入**: `query="foobar_not_exist_func"`
- **预期**: 回退到 Whoosh 关键词搜索补充

#### TC-CODE-005: 大小写不敏感搜索
- **前提**: 代码仓库已初始化
- **输入**: `query="secret_key"`（代码中为 SECRET_KEY）
- **预期**: 返回匹配行（-i 参数）

#### TC-CODE-006: 跨仓库搜索（不指定 repo_name）
- **前提**: 存在多个代码仓库
- **输入**: `query="def run"`
- **预期**: 搜索所有仓库并返回匹配

---

### 4.6 HR 系统 (hr_*)

#### TC-HR-001: 按姓名搜索员工
- **前提**: 数据库包含 50 名员工
- **输入**: `name="张"` → `hr_search_employees(name="张")`
- **预期**: 返回姓名包含 "张" 的员工列表

#### TC-HR-002: 按部门和职位组合搜索
- **输入**: `department="技术研发部"`, `title="工程师"` → `hr_search_employees(department="技术研发部", title="工程师")`
- **预期**: 返回满足两个条件的员工

#### TC-HR-003: 获取组织架构
- **输入**: `hr_get_org_chart()` 无参数
- **预期**: 返回所有部门及其负责人、人数

#### TC-HR-004: 获取指定部门组织架构
- **输入**: `hr_get_org_chart(department_id=1)`
- **预期**: 仅返回技术研发部（ID=1）的信息

#### TC-HR-005: 获取直属下属
- **前提**: 员工 ID=1 是管理者
- **输入**: `hr_get_direct_reports(manager_id=1)`
- **预期**: 返回 department_id 关联 ID=1 的 department 中 manager_id=1 的员工

#### TC-HR-006: 获取部门员工
- **输入**: `hr_get_department_employees(department_name="技术研发部")`
- **预期**: 返回该部门的所有员工

#### TC-HR-007: 无参数全量搜索
- **输入**: `hr_search_employees()` （无任何过滤条件）
- **预期**: 返回 top_k=10 条员工记录

---

### 4.7 CRM 系统 (crm_*)

#### TC-CRM-001: 按名称搜索客户
- **输入**: `crm_search_customers(name="科技")`
- **预期**: 返回客户名包含 "科技" 的记录

#### TC-CRM-002: 按行业搜索客户
- **输入**: `crm_search_customers(industry="金融")`
- **预期**: 返回金融行业客户

#### TC-CRM-003: 获取客户订单
- **前提**: 数据库包含订单
- **输入**: `crm_get_customer_orders(customer_id=1)`
- **预期**: 返回该客户的所有订单，含 total_amount、sales_rep、item_count

#### TC-CRM-004: 搜索产品
- **输入**: `crm_search_products(name="数据平台")`
- **预期**: 返回匹配产品，含 sku/category/unit_price/stock_quantity

#### TC-CRM-005: 按类别搜索产品
- **输入**: `crm_search_products(category="云服务")`
- **预期**: 返回该类别下的所有产品

#### TC-CRM-006: 无匹配客户
- **输入**: `crm_search_customers(name="不存在的公司XYZ")`
- **预期**: 返回空列表

---

### 4.8 文档管理系统 (doc_*)

#### TC-DOC-001: 按标题搜索文档
- **输入**: `doc_search_documents(title="信息安全")`
- **预期**: 返回标题包含 "信息安全" 的文档

#### TC-DOC-002: 按类型过滤
- **输入**: `doc_search_documents(doc_type="policy")`
- **预期**: 仅返回 policy 类型文档

#### TC-DOC-003: 按部门过滤
- **输入**: `doc_search_documents(department="技术研发部")`
- **预期**: 仅返回技术研发部发布的文档

#### TC-DOC-004: 组合搜索
- **输入**: `doc_search_documents(title="管理", doc_type="policy", department="技术研发部")`
- **预期**: 返回满足三个条件的文档

#### TC-DOC-005: 按类型获取全部文档
- **输入**: `doc_get_documents_by_type(doc_type="report")`
- **预期**: 返回所有 report 类型文档（上限 50）

#### TC-DOC-006: 无效文档类型
- **输入**: `doc_get_documents_by_type(doc_type="invalid_type")`
- **预期**: 返回空列表

---

### 4.9 项目管理系统 (pm_*)

#### TC-PM-001: 按名称搜索项目
- **输入**: `pm_search_projects(name="智慧城市")`
- **预期**: 返回匹配项目，含 manager、task_count

#### TC-PM-002: 按状态搜索
- **输入**: `pm_search_projects(status="in_progress")`
- **预期**: 仅返回进行中的项目

#### TC-PM-003: 获取项目任务
- **输入**: `pm_get_project_tasks(project_id=1)`
- **预期**: 返回该项目的所有任务，含 assignee、estimated_hours、actual_hours

#### TC-PM-004: 获取团队工作负载
- **输入**: `pm_get_team_workload(department_id=1)`
- **预期**: 返回该部门成员的任务数和预估工时汇总

#### TC-PM-005: 组合搜索
- **输入**: `pm_search_projects(name="系统", status="completed")`
- **预期**: 返回名称包含 "系统" 且已完成的项目

#### TC-PM-006: 无效状态值
- **输入**: `pm_search_projects(status="invalid_status")`
- **预期**: 返回空列表（无匹配）

---

## 5. Agent 决策层测试用例

### 5.1 Tool Registry

#### TC-TR-001: 全部工具注册
- **测试方法**: `tool_registry.get_tool_names()`
- **预期**: 返回 20 个工具名称（5 通用搜索 + 4 HR + 3 CRM + 2 文档 + 3 PM）

#### TC-TR-002: 已注册工具执行
- **测试方法**: `await tool_registry.execute("search_relational_db", query="测试")`
- **预期**: 正常返回结果列表

#### TC-TR-003: 未注册工具调用
- **测试方法**: `await tool_registry.execute("nonexistent_tool", query="test")`
- **预期**: 返回 `[{"error": "Unknown tool: nonexistent_tool"}]`

#### TC-TR-004: 工具异常处理
- **测试方法**: 传入错误的参数类型（如 query 传 int）
- **预期**: 返回 `[{"error": "..."}]`，不抛出未捕获异常

---

### 5.2 Agent Loop (核心推理循环)

#### TC-AGENT-001: 单轮搜索 — 人员查询 → 直接回答
- **输入**: `"技术总监是谁？"`
- **期望推理路径**: 识别为人事问题 → 调用 `search_relational_db` 或 `hr_search_employees` → 获取结果 → 输出最终答案
- **验证点**: iteration <= 3，final_answer 包含人员信息

#### TC-AGENT-002: 单轮搜索 — 文档查询 → 直接回答
- **输入**: `"公司信息安全管理制度的内容是什么？"`
- **期望推理路径**: 识别为文档类 → 调用 `search_vector_store` 或 `doc_search_documents` → 获取文档内容 → 输出最终答案
- **验证点**: final_answer 引用具体文档

#### TC-AGENT-003: 单轮搜索 — 代码查询 → 直接回答
- **输入**: `"ETLPipeline 类在哪个文件里？"`
- **期望推理路径**: 识别为代码问题 → 调用 `search_code` → 返回文件路径 → 输出最终答案
- **验证点**: final_answer 包含正确的文件路径

#### TC-AGENT-004: 多轮迭代 — 关联搜索
- **输入**: `"负责智慧城市管理平台的项目经理是谁？他手下有哪些人？"`
- **期望推理路径**:
  1. 第1轮: 调用 `pm_search_projects(name="智慧城市")` 获取项目经理 ID
  2. 第2轮: 调用 `hr_get_direct_reports(manager_id=...)` 获取下属
  3. 第3轮: 信息足够，输出答案
- **验证点**: iteration >= 2，最终答案回答了全部子问题

#### TC-AGENT-005: 多轮迭代 — 跨系统搜索
- **输入**: `"技术研发部正在进行哪些项目？这些项目的负责人分别是谁？"`
- **期望推理路径**:
  1. 第1轮: 调用 `hr_get_department_employees` 或直接查项目
  2. 第2轮: 调用 `pm_search_projects` 关联获取项目
  3. 后续轮: 补充负责人信息
- **验证点**: 结果跨 HR + PM 两个系统

#### TC-AGENT-006: 多轮迭代 — 宽泛问题需要逐步缩小
- **输入**: `"最近系统有没有出过什么问题？"`
- **期望推理路径**:
  1. 第1轮: 调用 `search_logs(level="ERROR", days_back=7)` 搜索错误日志
  2. 第2轮: 根据日志中的模块/服务名，进一步搜索相关代码或文档
  3. 输出总结
- **验证点**: 至少一次 log 搜索，答案包含具体错误描述

#### TC-AGENT-007: 无结果时换策略
- **输入**: `"朱啸云的联系方式？"`（数据库中不存在的人名）
- **期望推理路径**:
  1. 第1轮: `hr_search_employees(name="朱啸云")` → 无结果
  2. 第2轮: 换 `search_relational_db(query="朱啸云")` → 无结果
  3. 第3轮: 输出 "未找到该人员信息"
- **验证点**: 模型明确告知未找到，非虚假编造

#### TC-AGENT-008: 达到最大迭代次数
- **前提**: `agent_max_iterations=10`
- **输入**: 一个需要大量搜索但实际无法收集全部信息的宽泛问题
- **预期**: 在第10轮后输出 "已达到最大搜索轮次"
- **验证点**: AgentEvent type="final_answer"，content 包含最大轮次提示

#### TC-AGENT-009: 模糊问题 → 使用向量搜索
- **输入**: `"怎么处理客户投诉？"`
- **期望推理路径**: 识别为流程/规范类概念性问题 → 优先 `search_vector_store` 或 `doc_search_documents`
- **验证点**: 第一次工具调用为向量搜索或文档搜索

#### TC-AGENT-010: 精确 ID 查询 → 使用企业 SDK
- **输入**: `"客户ID 3 的订单情况？"`
- **期望推理路径**: 识别为精确 ID 查询 → 调用 `crm_get_customer_orders(customer_id=3)`
- **验证点**: 第一次调用包含 customer_id 参数

#### TC-AGENT-011: 超时处理
- **前提**: 模拟模型响应超时（超过30秒）
- **输入**: 任意问题
- **预期**: AgentEvent type="error"，data.message="模型响应超时"
- **验证点**: 不导致服务崩溃

#### TC-AGENT-012: 工具执行超时
- **前提**: 模拟工具执行超时（超过15秒）
- **输入**: 触发长时间运行的工具
- **预期**: 返回 `[{"error": "工具执行超时"}]`

---

### 5.3 Tracer（追踪系统）

#### TC-TRACE-001: 完整对话追踪
- **测试方法**: 
  1. `tracer.start_trace(conv_id, query)`
  2. 添加若干 TraceStep
  3. `tracer.complete_trace(conv_id, final_answer)`
  4. `tracer.get_trace(conv_id)`
- **预期**: 返回按时间顺序排列的步骤列表，包含 tool_call → tool_result → ... → final_answer

#### TC-TRACE-002: 不存在对话 ID 的追踪
- **输入**: `tracer.get_trace("nonexistent_id")`
- **预期**: 返回空列表 `[]`

#### TC-TRACE-003: TraceStep 序列化
- **测试方法**: `TraceStep(step_number=1, event_type="tool_call", tool_name="test", arguments={"q":"x"}).to_dict()`
- **预期**: 字典包含所有字段，timestamp 为 ISO 格式字符串

---

## 6. API 层集成测试用例

### 6.1 /api/chat (SSE 流式对话)

#### TC-API-001: 正常对话流程
- **请求**: `POST /api/chat` body: `{"query": "张三是谁？", "conversation_id": ""}`
- **预期响应**:
  - event: conversation_id → data: {"conversation_id": "uuid"}
  - event: tool_call → data: {tool_name, arguments, iteration}
  - event: tool_result → data: {tool_name, result_summary, result_count, iteration}
  - event: final_answer → data: {content, iterations, trace}
  - event: done
- **验证点**: SSE 流正常推送，Content-Type 为 text/event-stream

#### TC-API-002: 带已有 conversation_id 的对话
- **请求**: `POST /api/chat` body: `{"query": "再查查他的部门", "conversation_id": "known-uuid"}`
- **预期**: 使用已有 conversation_id，追踪同一会话

#### TC-API-003: 并发请求
- **请求**: 同时发送 3 个不同的查询
- **预期**: 3 个请求独立处理，互不干扰，各自返回独立 conversation_id

#### TC-API-004: 空查询
- **请求**: `POST /api/chat` body: `{"query": ""}`
- **预期**: 模型应能处理或返回合理响应（不应 500 崩溃）

#### TC-API-005: 超长查询
- **请求**: `POST /api/chat` body: `{"query": "（5000字符的长文本）"}`
- **预期**: 正常处理或被合理截断

---

### 6.2 /api/conversations

#### TC-API-006: 列出所有对话
- **请求**: `GET /api/conversations`
- **预期**: 返回 conversations 列表

#### TC-API-007: 获取特定对话详情
- **请求**: `GET /api/conversations/{known_id}`
- **预期**: 返回该对话的完整步骤追踪

#### TC-API-008: 获取不存在的对话
- **请求**: `GET /api/conversations/nonexistent-id`
- **预期**: HTTP 404

---

### 6.3 /api/search/direct (直接搜索)

#### TC-API-009: 直接搜索 — relational
- **请求**: `POST /api/search/direct` body: `{"query": "项目", "engine": "relational", "top_k": 5}`
- **预期**: 返回搜索结果列表

#### TC-API-010: 直接搜索 — vector
- **请求**: `POST /api/search/direct` body: `{"query": "安全管理", "engine": "vector", "top_k": 5}`
- **预期**: 返回向量搜索结果

#### TC-API-011: 直接搜索 — keyword
- **请求**: `POST /api/search/direct` body: `{"query": "数据安全", "engine": "keyword", "top_k": 5}`
- **预期**: 返回关键词搜索结果

#### TC-API-012: 直接搜索 — log
- **请求**: `POST /api/search/direct` body: `{"query": "超时", "engine": "log", "top_k": 5}`
- **预期**: 返回日志搜索结果

#### TC-API-013: 直接搜索 — code
- **请求**: `POST /api/search/direct` body: `{"query": "hash_password", "engine": "code", "top_k": 5}`
- **预期**: 返回代码搜索结果

#### TC-API-014: 无效引擎
- **请求**: `POST /api/search/direct` body: `{"query": "test", "engine": "invalid"}`
- **预期**: HTTP 400, detail="Unknown engine: invalid"

---

### 6.4 /api/stats

#### TC-API-015: 获取系统统计
- **请求**: `GET /api/stats`
- **预期**: 返回 `{tool_count, vector_collections, tools}`

---

## 7. 数据层测试用例

### 7.1 数据初始化

#### TC-DATA-001: 种子数据完整性
- **测试方法**: 运行 `seed_all()` 后检查各表记录数
- **预期**:
  - departments: 5 条
  - employees: 50 条
  - projects: 8 条
  - tasks: 40 条
  - customers: 30 条
  - products: 20 条
  - orders: 60 条
  - order_items: >=60 条
  - documents: 25 条
  - log_entries: 500 条

#### TC-DATA-002: ChromaDB 索引完整性
- **测试方法**: 运行 `seed_chroma()` 后检查
- **预期**: documents collection 包含 25 个文档向量

#### TC-DATA-003: Whoosh 索引完整性
- **测试方法**: 运行 `seed_whoosh()` 后检查
- **预期**: 索引包含 >=225 条记录（25 文档 + 200 日志）

#### TC-DATA-004: 关联外键完整性
- **测试方法**: 检查 employee.department_id 是否存在对应 department
- **预期**: 所有外键引用有效

#### TC-DATA-005: 重复初始化（幂等性）
- **测试方法**: 连续运行两次 `seed_all()`
- **预期**: 第二次运行不会导致数据翻倍或崩溃

---

### 7.2 模型定义

#### TC-MODEL-001: Employee 模型字段
- **验证**: name, email, phone, title, department_id, manager_id, hire_date, salary, status 均可正常读写

#### TC-MODEL-002: Project 模型字段
- **验证**: name, description, status (planning/in_progress/delayed/completed/cancelled), priority (low/medium/high/critical), budget

#### TC-MODEL-003: LogEntry 模型字段
- **验证**: timestamp, level (DEBUG/INFO/WARNING/ERROR/CRITICAL), module, function_name, message, traceback, request_id

#### TC-MODEL-004: Document 模型字段
- **验证**: title, doc_type, department, author, created_date, tags, content, status

---

## 8. 前端 UI 测试用例

#### TC-UI-001: 页面加载
- **测试步骤**: 访问 `http://localhost:8000/`
- **预期**: 加载 index.html，显示搜索界面

#### TC-UI-002: 发送消息
- **测试步骤**: 在输入框输入问题，点击发送
- **预期**: 显示用户消息，开始 SSE 连接，逐步展示搜索过程和最终答案

#### TC-UI-003: 展示搜索结果摘要
- **测试步骤**: 发送一个触发多轮搜索的问题
- **预期**: UI 实时显示每一轮的工具调用和结果摘要（tool_call → tool_result → ... → final_answer）

#### TC-UI-004: 空输入提交
- **测试步骤**: 输入框为空时点击发送
- **预期**: 阻止提交或提示输入

#### TC-UI-005: 连续对话
- **测试步骤**: 发送问题1 → 收到答案 → 发送问题2
- **预期**: 两次对话使用不同 conversation_id，UI 保持流畅

---

## 9. 性能测试用例

#### TC-PERF-001: 关系型搜索响应时间
- **输入**: `search_relational_db(query="张", top_k=10)`
- **预期**: 执行时间 < 500ms

#### TC-PERF-002: 向量搜索响应时间
- **输入**: `search_vector_store(query="安全管理制度", top_k=10)`
- **预期**: 执行时间 < 1s（含嵌入向量生成）

#### TC-PERF-003: 关键词搜索响应时间
- **输入**: `search_keywords(query="数据", top_k=10)`
- **预期**: 执行时间 < 500ms

#### TC-PERF-004: 日志搜索响应时间
- **输入**: `search_logs(query="错误", top_k=10, days_back=30)`
- **预期**: 执行时间 < 1s

#### TC-PERF-005: 代码搜索响应时间（跨仓库）
- **输入**: `search_code(query="def ")`
- **预期**: 执行时间 < 2s

#### TC-PERF-006: 单轮 Agent 对话端到端
- **输入**: 一个简单问题（预期1轮就结束）
- **预期**: 总响应时间 < 10s

#### TC-PERF-007: 多轮 Agent 对话端到端
- **输入**: 一个需要 3-4 轮搜索的复杂问题
- **预期**: 总响应时间 < 30s

#### TC-PERF-008: 并发工具调用
- **测试方法**: 同时触发 5 个独立工具调用
- **预期**: 所有调用正常完成，无竞态条件

---

## 10. 错误处理与边界测试

#### TC-ERR-001: 数据库连接断开
- **测试方法**: 删除或移动 SQLite 数据库文件后搜索
- **预期**: 返回包含错误信息的结果，不崩溃

#### TC-ERR-002: ChromaDB 数据损坏
- **测试方法**: 删除 chroma.sqlite3 后搜索
- **预期**: 自动重建，返回空结果

#### TC-ERR-003: Whoosh 索引丢失
- **测试方法**: 删除 whoosh_index 目录后搜索
- **预期**: 自动创建空索引，返回空结果

#### TC-ERR-004: 代码仓库目录丢失
- **测试方法**: 删除 mock_repos 目录后搜索
- **预期**: 返回空结果（list_repos() 返回 []）

#### TC-ERR-005: DeepSeek API Key 无效
- **前提**: .env 中配置无效 API Key
- **输入**: 任意问题
- **预期**: 返回明确认证错误，不暴露 API Key

#### TC-ERR-006: DeepSeek API 不可达
- **前提**: 修改 API base URL 为不可达地址
- **输入**: 任意问题
- **预期**: 返回网络错误或超时错误

#### TC-ERR-007: 极长查询字符串
- **输入**: 传入 10000 字符的查询
- **预期**: 各搜索引擎能处理（截断或正常搜索）

#### TC-ERR-008: 特殊字符查询
- **输入**: `query="DROP TABLE; --"`, `/../../etc/passwd`, `"><script>alert(1)</script>`
- **预期**: 不产生 SQL 注入、路径遍历、XSS（参数化查询或合理转义）

#### TC-ERR-009: Unicode / Emoji 查询
- **输入**: `query="😀🎉测试"`
- **预期**: 正常搜索（至少不崩溃）

---

## 11. 决策推理质量测试

这些测试用例不需要精确编程验证，适合人工评估或 LLM-as-judge 方式评测。

#### TC-REASON-001: 问题类型识别 — 人员
- **输入**: `"李明在哪个部门？他的上级是谁？"`
- **理想推理路径**: HR系统 → 获取部门 → 获取上级
- **劣化标记**: 首次调用为代码搜索或日志搜索

#### TC-REASON-002: 问题类型识别 — 政策文档
- **输入**: `"公司对数据安全有什么规定？"`
- **理想推理路径**: 向量搜索/文档搜索 → 获取政策文档
- **劣化标记**: 首次调用为代码仓库搜索

#### TC-REASON-003: 问题类型识别 — 代码
- **输入**: `"ETLPipeline 的 run 方法做了什么？"`
- **理想推理路径**: 代码仓库搜索 → 获取源代码
- **劣化标记**: 首次调用为 HR 系统或 CRM 系统

#### TC-REASON-004: 问题类型识别 — 故障排查
- **输入**: `"为什么昨天支付服务一直报错？"`
- **理想推理路径**: 日志搜索（ERROR级别，支付相关）→ 如需要可进一步查代码
- **劣化标记**: 未使用日志搜索

#### TC-REASON-005: 问题类型识别 — 项目进度
- **输入**: `"智慧城市平台目前进度如何？谁在负责？"`
- **理想推理路径**: PM系统 → 如有需要补充查HR系统获取人员详情
- **劣化标记**: 未使用PM系统工具

#### TC-REASON-006: 信息不足时诚实告知
- **输入**: `"火星分公司的营业额是多少？"`（数据中不存在）
- **期望行为**: 多轮搜索后明确告知未找到相关数据
- **劣化标记**: 编造数据或给出模糊的通用回答

#### TC-REASON-007: 引用来源
- **输入**: `"公司信息安全制度的内容"`
- **期望行为**: 答案中引用 "根据《信息安全管理制度》文档..." 或 "根据文档管理系统中的《XXX》..."
- **劣化标记**: 答案无任何来源引用

---

## 12. 端到端 (E2E) 场景测试

#### TC-E2E-001: 新人入职综合查询
- **场景**: 新员工想知道自己的工作安排
- **输入**: `"我是新来的后端工程师，分到了技术研发部。我们部门正在做什么项目？我的直属上级是谁？公司有哪些技术规范我需要看？"`
- **理想流程**:
  1. `hr_search_employees(title="后端工程师", department="技术研发部")` → 找到自己
  2. `pm_search_projects` → 获取部门项目
  3. `hr_get_org_chart(department_id=1)` → 获取组织架构
  4. `doc_search_documents(doc_type="spec", department="技术研发部")` 或 `search_vector_store(query="技术规范")` → 获取技术规范
- **验证**: 回答涵盖项目信息 + 上级信息 + 规范文档

#### TC-E2E-002: 客户订单异常排查
- **场景**: 客服发现某客户订单出问题
- **输入**: `"金融行业的'XX科技'客户，最近有一笔订单支付失败。帮我查查这个客户的订单状态、是谁负责的、系统有没有相关错误日志"`
- **理想流程**:
  1. `crm_search_customers(industry="金融")` → 找到客户
  2. `crm_get_customer_orders(customer_id=X)` → 获取订单
  3. `search_logs(level="ERROR", query="支付")` → 查找错误日志
- **验证**: 回答包含客户信息 + 订单状态 + 错误日志 + 负责人

#### TC-E2E-003: 项目管理决策支持
- **场景**: 管理层想了解项目资源情况
- **输入**: `"技术研发部现在有哪些项目在拖延？项目成员的工作负载如何？有没有人可以调去支援紧急项目？"`
- **理想流程**:
  1. `pm_search_projects(status="delayed")` → 找到延迟项目
  2. `pm_get_team_workload(department_id=1)` → 获取工作负载
  3. 分析和推荐
- **验证**: 答案包含延迟项目列表 + 工作负载分析 + 人员调配建议

#### TC-E2E-004: 代码审计辅助
- **场景**: 安全审计需要检查代码
- **输入**: `"公司代码里有没有硬编码的密码或者密钥？数据加密是怎么实现的？"`
- **理想流程**:
  1. `search_code(query="secret")` 或 `search_code(query="password")` → 找硬编码
  2. `search_code(query="encrypt")` 或 `search_code(query="hash")` → 找加密实现
  3. 可能补充 `search_vector_store(query="数据加密安全")` → 查阅安全规范
- **验证**: 回答包含代码证据 + 安全规范引用

---

## 13. 测试执行策略

### 13.1 优先级定义

| 优先级 | 含义 | 适用用例 |
|--------|------|----------|
| P0 | 阻塞性问题，必须通过 | TC-AGENT-001~012, TC-TR-001~004, TC-API-001, TC-ERR-001~006 |
| P1 | 核心功能，高优先级 | TC-RDB-*, TC-VEC-*, TC-KW-*, TC-LOG-*, TC-CODE-*, TC-HR-*, TC-CRM-*, TC-DOC-*, TC-PM-* |
| P2 | 重要但非阻塞 | TC-API-*, TC-DATA-*, TC-MODEL-*, TC-E2E-* |
| P3 | 增强质量 | TC-UI-*, TC-PERF-*, TC-REASON-*, TC-ERR-007~009 |

### 13.2 建议执行顺序

```
阶段1: 单元测试（搜索引擎 + 企业SDK）    →  阶段2: Agent决策层测试
    ↓                                              ↓
阶段3: API集成测试                       →  阶段4: E2E场景测试
    ↓                                              ↓
阶段5: 性能测试                          →  阶段6: 推理质量评估（人工）
```

### 13.3 自动化建议

| 层级 | 工具 | 覆盖范围 |
|------|------|----------|
| 单元测试 | pytest + pytest-asyncio | 搜索引擎、企业SDK、ToolRegistry、Tracer |
| API 测试 | httpx + pytest | /api/chat, /api/search/direct, /api/stats |
| E2E 测试 | Playwright | Web UI 交互流程 |
| 性能测试 | pytest-benchmark | 搜索响应时间 |

---

## 14. 测试通过标准

| 维度 | 标准 |
|------|------|
| P0 用例通过率 | 100% |
| P1 用例通过率 | >= 95% |
| P2 用例通过率 | >= 90% |
| P3 用例通过率 | >= 80% |
| Agent 单轮对话时间 | P95 < 10s |
| Agent 多轮对话时间 (3-4轮) | P95 < 30s |
| 并发 5 请求 | 无崩溃，无数据交叉污染 |
| 决策合理性 (TC-REASON-*) | 人工评估通过率 >= 85% |

---

> **附录**: 测试数据由 `scripts/seed_all.py` + `scripts/build_mock_repos.py` 生成，使用固定随机种子 (seed=42)，确保每次初始化得到相同数据，测试结果可复现。
